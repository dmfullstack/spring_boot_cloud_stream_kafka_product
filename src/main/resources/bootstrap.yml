spring:
  application:
    name: kafkaproductdemo
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config
eureka:
  instance:
    non-secure-port: ${server.port:8089}
  client:
    service-url:
      defaultZone: http://zsl:123@${eureka.host:localhost}:${eureka.port:8761}/eureka/